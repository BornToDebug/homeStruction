<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lilla.homestruction">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateVisible|adjustPan">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.MainScreen"
            android:label="HomeStruction"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.ForgottenPassword"
            android:windowSoftInputMode="stateVisible|adjustPan"
            android:label="HomeStruction"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.TemperatureScreen"
            android:label="HomeStruction"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.LightScreen"
            android:label="HomeStruction"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.HumidityScreen"
            android:label="HomeStruction"/>

        <!-- ATTENTION: This was auto-generated to add Google Play services to your project for
        App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

    </application>

</manifest>